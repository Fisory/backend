import { Router } from 'express';import {  getUploadTokenHandler,  createVideoHandler,  getVideoListHandler,  getVideoDetailHandler,  updateVideoHandler,  deleteVideoHandler,  publishVideoHandler,  getUserVideosHandler,} from '@/controllers/video.controller';import { authMiddleware, optionalAuthMiddleware } from '@/middlewares/auth.middleware';const router = Router();router.get('/upload-token', authMiddleware, getUploadTokenHandler);router.post('/', authMiddleware, createVideoHandler);router.get('/', getVideoListHandler);router.get('/user/:userId', getUserVideosHandler);router.get('/:id', optionalAuthMiddleware, getVideoDetailHandler);router.put('/:id', authMiddleware, updateVideoHandler);router.delete('/:id', authMiddleware, deleteVideoHandler);router.post('/:id/publish', authMiddleware, publishVideoHandler);export default router;
